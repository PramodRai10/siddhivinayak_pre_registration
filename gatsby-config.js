/**
 * Configure your Gatsby site with this file.
 *
 * See: https://www.gatsbyjs.com/docs/gatsby-config/
 */

const { createProxyMiddleware } = require("http-proxy-middleware")

module.exports = {
  /* Your site config here */
  plugins: [],
  siteMetadata:{
    title:'Dojo',
    description:"It's a dojo"
  }
}
